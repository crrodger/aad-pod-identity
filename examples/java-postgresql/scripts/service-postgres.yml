apiVersion: v1
kind: Service
metadata:
  name: demo-postgresql
  namespace: <INSERT_NAMESPACE_HERE>
spec:
  type: LoadBalancer
  selector:
    name: demo-postgresql-deployment
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080